# دليل التثبيت - محرر فيديو القرآن الكريم

## للمستخدمين النهائيين

### Windows

#### التثبيت من الملف التنفيذي (موصى به)

1. **تحميل التطبيق**
   - قم بتحميل ملف `Quran-Video-Editor-Setup-1.0.0.exe`
   - الحجم التقريبي: ~50 ميجابايت (بدون FFmpeg!)

2. **التثبيت**
   - قم بتشغيل ملف Setup
   - اتبع خطوات التثبيت
   - اختر مجلد التثبيت (اختياري)
   - انتظر اكتمال التثبيت

3. **التشغيل**
   - قم بتشغيل التطبيق من سطح المكتب أو قائمة Start
   - التطبيق جاهز للاستخدام فوراً!

#### التشغيل Portable (بدون تثبيت)

1. قم بتحميل ملف `Quran-Video-Editor-Portable-1.0.0.zip`
2. فك الضغط في أي مجلد
3. قم بتشغيل `Quran Video Editor.exe`

### المتطلبات

- ✅ Windows 10 أو أحدث (x64)
- ✅ 4 جيجابايت رام (8 جيجابايت موصى به)
- ✅ 100 ميجابايت مساحة فارغة
- ✅ مفتاح OpenAI API (للتعرف على الكلمات)

### إعداد OpenAI API

1. **الحصول على مفتاح API**
   - سجل في https://platform.openai.com
   - انتقل إلى API Keys
   - أنشئ مفتاح جديد

2. **إضافة المفتاح للتطبيق**
   
   **الطريقة 1: متغيرات البيئة (موصى بها)**
   ```cmd
   setx OPENAI_API_KEY "your-api-key-here"
   ```
   ثم أعد تشغيل التطبيق

   **الطريقة 2: ملف .env**
   - أنشئ ملف `.env` في مجلد التطبيق
   - أضف السطر: `OPENAI_API_KEY=your-api-key-here`

## للمطورين

### التثبيت من الكود المصدري

#### المتطلبات
- Node.js 18 أو أحدث
- npm أو yarn
- Git

#### خطوات التثبيت

1. **استنساخ المشروع**
   ```bash
   git clone https://github.com/username/quran-video-editor.git
   cd quran-video-editor
   ```

2. **تثبيت الاعتماديات**
   ```bash
   npm install
   ```

3. **إعداد متغيرات البيئة**
   ```bash
   # في Linux/macOS
   export OPENAI_API_KEY="your-api-key-here"
   
   # في Windows CMD
   set OPENAI_API_KEY=your-api-key-here
   
   # في Windows PowerShell
   $env:OPENAI_API_KEY="your-api-key-here"
   ```

4. **التشغيل في وضع التطوير**
   ```bash
   npm run dev
   ```
   ثم في terminal آخر:
   ```bash
   npm run electron
   ```

5. **بناء التطبيق**
   ```bash
   npm run build
   ```

6. **إنشاء ملف التوزيع**
   ```bash
   # لنظام Windows
   npm run dist:win
   
   # لنظام macOS (على Mac فقط)
   npm run dist
   
   # للجميع
   npm run pack
   ```

### البناء للتوزيع

#### Windows

```bash
npm run dist:win
```

سيتم إنشاء الملفات في:
- `release/Quran-Video-Editor-Setup-1.0.0.exe` - مثبت NSIS
- `release/win-unpacked/` - ملفات portable

#### macOS (قريباً)

```bash
npm run dist:mac
```

#### Linux (قريباً)

```bash
npm run dist:linux
```

### حل المشاكل الشائعة

#### مشكلة: "FFmpeg.dll not found"
**الحل**: هذه المشكلة لم تعد موجودة! التطبيق الآن لا يستخدم FFmpeg نهائياً.

#### مشكلة: "OPENAI_API_KEY is not configured"
**الحل**: تأكد من إعداد مفتاح OpenAI API في متغيرات البيئة.

#### مشكلة: بطء في المعالجة
**الحل**: 
- استخدم فيديو أقل من 500 ميجابايت
- قلل الدقة إلى 720p
- أغلق التطبيقات الأخرى

#### مشكلة: لا يعمل التطبيق
**الحل**:
- تأكد من تثبيت Windows 10 أو أحدث
- جرب تشغيل التطبيق كمسؤول
- تأكد من تثبيت Visual C++ Redistributable

### الاختبار

```bash
# اختبار البناء
npm run build

# اختبار التشغيل
npm start

# اختبار التوزيع
npm run pack
```

## إلغاء التثبيت

### Windows

1. **من لوحة التحكم**
   - Settings → Apps → Quran Video Editor → Uninstall

2. **يدوياً**
   - احذف مجلد التطبيق
   - احذف الاختصارات من سطح المكتب وقائمة Start

### حذف البيانات

```cmd
# حذف البيانات المؤقتة
rmdir /s /q %TEMP%\quran-video-editor

# حذف الإعدادات
rmdir /s /q %APPDATA%\quran-video-editor
```

## الترقية

### تحديث التطبيق

1. قم بتحميل النسخة الجديدة
2. قم بتشغيل المثبت
3. سيتم تحديث التطبيق تلقائياً

### الترقية من إصدار يستخدم FFmpeg

1. قم بإلغاء تثبيت النسخة القديمة
2. قم بتثبيت النسخة الجديدة
3. لن تحتاج إلى FFmpeg بعد الآن!

## الدعم

### الحصول على المساعدة

- 📖 اقرأ [README.md](README.md) للمعلومات الأساسية
- 🔧 اقرأ [DEVELOPERS.md](DEVELOPERS.md) للتفاصيل التقنية
- 🐛 أبلغ عن الأخطاء في GitHub Issues
- 💬 اطرح الأسئلة في GitHub Discussions

### معلومات الإصدار

- **الإصدار الحالي**: 1.0.0
- **تاريخ الإصدار**: 2024
- **التغييرات**: انظر [CHANGELOG.md](CHANGELOG.md)

---

**شكراً لاستخدام محرر فيديو القرآن الكريم** ❤️

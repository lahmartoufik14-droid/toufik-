# npm configuration for Quran Video Editor
# This ensures proper installation without FFmpeg

# Prevent optional dependencies that might pull FFmpeg
optional=false

# Use exact versions
save-exact=true
